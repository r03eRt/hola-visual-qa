# Implement one feature

Implement only `<feature-slug>` according to `docs/features/<feature-slug>/SPEC.md` and the canonical specifications. Keep changes independently reviewable. Add deterministic tests, preserve module boundaries, do not alter unrelated baselines and update `docs/STATUS.md`. Finish with acceptance-criteria results and handover notes.
