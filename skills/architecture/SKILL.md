# Architecture skill

Keep domain/provider boundaries, introduce interfaces only at real variation points, avoid circular dependencies and ensure reporting/AI consume normalized results rather than Playwright internals.
