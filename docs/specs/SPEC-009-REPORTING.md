# SPEC-009 Reporting

Generate:

- machine-readable `manifest.json` and `summary.json`;
- scenario-level result JSON;
- local static HTML report linking evidence;
- concise CLI summary and correct exit code.

The report groups failures by page and probable category, displays scenario dimensions, shows visual triplets and makes AI content visually distinct from deterministic facts. It must work when AI is disabled or unavailable.
