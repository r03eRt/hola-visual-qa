# Current implementation status

Status labels: `scaffold`, `partial`, `implemented`, `verified`.

| Capability | Status | Notes |
|---|---|---|
| Repository bootstrap | implemented | Dependency versions are exact-pinned to mutually-compatible, lockfile-resolved versions (no `latest`, no ranges, no npm hacks): `typescript@5.9.3` and `@types/node@22.20.1` (downgraded from TS 7/`@types/node` 26 to stay compatible with `typescript-eslint@8.65.0`), `eslint@10.8.0`, `@eslint/js@10.0.1`, `@anthropic-ai/sdk@0.115.0`, `dotenv@17.4.2`, `zod@4.4.3`, `@playwright/test@1.62.1`, `tsx@4.23.1`. `npm ci` succeeds cleanly from the committed lockfile with no `.npmrc`/`legacy-peer-deps` and no package aliases. `eslint.config.js` (flat config, `@eslint/js` + `typescript-eslint` recommended, scoped to `src/`, `tests/`, `scripts/`, `*.config.ts`) is in place and `npm run lint` exits 0 (one real unused-catch-binding violation fixed in `scripts/new-ticket.mjs`). `npm run typecheck` exits 0. TS 7 remains unpinned for now — revisit as a separate chore once `typescript-eslint` ships support (tracked upstream). |
| Project configuration contract | implemented | `src/config/schema.ts` defines a strict, versioned Zod `ProjectConfig` (unknown keys fail on every object); `src/config/load-config.ts#loadConfig()` validates the committed `visual-qa.config.ts` or a supplied object, aggregates all validation issues into one thrown `ConfigValidationError`, and applies only an explicit env allowlist (`BASE_URL`, `TEST_COUNTRY`) — no secret ever enters the returned config. Verified by `tests/unit/config.spec.ts` (22 assertions across both Playwright projects), `npm run typecheck` and `npm run lint` (both exit 0). Scenario expansion (turning `dimensions` into concrete scenarios) is still pending — see next row. |
| Domain result/error contracts | implemented | `src/domain/error.ts` defines the 12 `ErrorCategory` values, the `Phase` union and `normalizeError()`, which produces a stable `code`, ISO `timestamp` and a redacted, stack-free, report-safe `message` (auth headers, cookie values and `sk-`/api-key-like tokens are stripped); `ai_provider_error` defaults to `severity: 'warning'` unless overridden. `src/domain/result.ts` defines provider-neutral `Scenario`, `ArtifactRefs` (relative paths only), `ScenarioResult`, `RunManifest`, `RunResult` and `RunSummary`, all as strict Zod schemas (`z.output` is the single source of truth) that reject unknown keys and any secret-looking field (`apiKey`, `authorization`, `cookie`, `secret`, `password`, `token`, ...) at every object level; `computeDeterministicFailure()` is true iff a non-skipped result is `failed` or carries a non-warning error — AI warnings alone never trigger it. `src/domain/index.ts` barrels both modules. Purely additive; `src/scenarios/scenarios.ts` is untouched. Verified by `tests/unit/domain.spec.ts` (70 assertions across both Playwright projects), `npm run typecheck` and `npm run lint` (both exit 0). No path building, run-id generation, manifest writing or scenario expansion yet — that is later work. |
| Scenario matrix | partial | `src/scenarios/planner.ts#planScenarios()` deterministically expands `ProjectConfig` (`pages x device x consent x country x ads`) into domain `Scenario[]`, sorted by a stable, human-readable ID from `src/scenarios/id.ts#buildScenarioId()` (format `<page>-<device>-<consent>-<country>-<ads_on|ads_off>`, documented in that file's header comment; independent of input array order). `src/scenarios/filter.ts#matchesFilter()` applies include filters (`pages`, `tags`, `devices`, `consent`, `countries`, `ads`) that narrow the plan and `excludeCombinations` that remove matches (exclude always wins). `planScenarios()` throws a normalized `configuration_error`/`planning` `ScenarioPlanningError` before any browser work when the plan is empty after filtering, IDs collide, or the count exceeds `maxScenarios` (default `DEFAULT_MAX_SCENARIOS = 500`). `npm run plan` (`src/scenarios/plan-cli.ts`) is a dry-run that loads the config, applies `--page`/`--device`/`--country`/`--tag`/`--max` flags, prints the ordered plan and counts, and never launches a browser (`process.exit(1)` with a report-safe message on validation failure). Purely additive: `src/scenarios/scenarios.ts` and `tests/visual/*` are untouched, and the visual suite/orchestrator do not yet consume this planner. Verified by `tests/unit/scenario-planner.spec.ts` (covering expansion count/shape, ID stability under reordered dimension arrays, include/exclude filters, empty-plan error, max-guard error and duplicate-ID detection), `npm run typecheck` and `npm run lint` (both exit 0). Still pending: wiring the planner into the orchestrator/visual suite, `userFixture`/feature-flag dimensions, and a config-driven exclude list (currently a planner option only). |
| Artifact layout | partial | `src/artifacts/run-id.ts#newRunId()` produces sortable, filesystem-safe, unique run IDs (`<compact UTC timestamp>-<6-hex suffix>`, injectable clock, `isRunId()`/`RUN_ID_PATTERN` validators). `src/artifacts/paths.ts` reproduces `docs/architecture/ARTIFACT_MODEL.md` exactly, rooted at the configurable `ArtifactPolicy.outputDir` (not a hardcoded `artifacts/`): `ARTIFACT_FILENAMES` maps every `ArtifactRefs` key to its canonical filename; `runDir`/`manifestPath`/`summaryPath`/`reportIndexPath`/`scenarioDir`/`scenarioArtifactPath` build absolute paths defensively rejecting traversal/absolute IDs; `relativeArtifactRef`/`buildArtifactRefs` produce the relative `ArtifactRefsSchema`-valid refs a later capture feature will attach to a `ScenarioResult`. `src/artifacts/manifest.ts#buildRunManifest()`/`buildRunSummary()` assemble schema-valid `RunManifest`/`RunSummary` (via `parseRunManifest`/`parseRunSummary`); `configHash` is a stable sha256 over a recursively key-sorted canonical JSON form of `ProjectConfig` (order-independent, no secrets serialized — no browser is launched, `browser` is passed in by the caller). `src/artifacts/writer.ts` creates the run/scenario directory tree and writes `manifest.json`/`summary.json` atomically (temp file + `rename`), never outside `outputDir`, with no deletion/pruning. Verified by `tests/unit/artifacts.spec.ts` (56 assertions across both Playwright projects), `npm run typecheck` and `npm run lint` (both exit 0). Still pending: actual artifact **capture** (screenshots/console/network/trace/video), `report/index.html` rendering, and retention/pruning — none of that is implemented here, only the deterministic layout and manifest/summary writers later capture/reporting features will build on. |
| Browser launch/context factory | partial | `src/browser/` (new, additive): `devices.ts#DEVICE_PROFILES`/`deviceContextOptions()` is the single source of truth for per-device `BrowserContextOptions`, built from `devices['Desktop Chrome']` + `{viewport:1440x900}` and `devices['Pixel 7']` and asserted equal to `playwright.config.ts`'s `desktop-chromium`/`mobile-chromium` projects. `context-options.ts#buildContextOptions()` merges device profile → `{baseURL: config.baseUrl}` → caller overrides (overrides win), pure/no I/O. `launcher.ts#launchBrowser()` launches Chromium headless with a small, documented `DETERMINISTIC_ARGS` list. `context-factory.ts` defines minimal structural `BrowserLike`/`BrowserContextLike` interfaces and `createScenarioContext()`/`newScenarioPage()`/`withScenarioContext()` (the disposable entry point, `finally`-closes the context even when the callback throws). Verified by `tests/unit/browser.spec.ts` (DI with a fake browser/context, no real browser launched — `npx playwright test tests/unit` passes) and `npm run typecheck`/`npm run lint` (both exit 0). `tests/integration/browser-smoke.spec.ts` (real `launchBrowser()` + desktop/mobile viewport assertions) is written but could not be executed in this sandboxed dev environment — Chromium fails to spawn a renderer/page here with a Mach-port-rendezvous sandbox denial (`SIGSEGV`, unrelated to this feature's code; reproduced even with a bare Playwright `page` fixture and no custom launch args), so it must be run in CI or an unrestricted environment before being trusted. Still pending (later features): navigation, consent injection, page stabilization, artifact capture, and orchestration — none of that exists yet. |
| Consent engine | partial | `src/consent/` (new, additive — `consent-manager.ts` legacy scaffold untouched/unused): `adapter.ts` defines the SPEC-002 contract (`ConsentAdapter.apply/verify/describeRedacted`), `ConsentState`, the non-throwing `ConsentVerification` (`satisfied`/`present`/`expectedState` — never the raw cookie value) and minimal DI structural interfaces `ConsentContextLike`/`ConsentPageLike` that a real Playwright `BrowserContext`/`Page` satisfies. `cookie-adapter.ts#CookieConsentAdapter` implements the cookie strategy: `apply()` adds exactly one cookie matching the legacy scaffold's shape (`value = state` by default, `path:'/'`, `secure:true`, `sameSite:'Lax'`); `verify()` reads `page.context().cookies()` back and reports the *effective* state (present/satisfied), never assuming what was applied; `describeRedacted()` returns a report-safe descriptor with `cookieValue:'[redacted]'`. `createCookieConsentAdapter(scenario, config)` resolves `cookieName`/`cookieDomain` from `config.adapters.consent`, defaulting to `'consent_status'` and the `baseUrl` host. `assertConsentSatisfied()` throws a normalized `state_verification_error`/`state_verification` `ConsentVerificationError` with a redacted message (name/domain/expected state only) when not satisfied; `verify()` itself never throws. Verified by `tests/unit/consent-cookie-adapter.spec.ts` (30 assertions across both Playwright projects, DI only, no real browser), `npm run typecheck` and `npm run lint` (both exit 0). Additive (#17): `ui-types.ts` defines `ConsentLocatorLike`/`ConsentUiPageLike`/`ConsentVerifyPageLike` DI structural interfaces that a real Playwright `Page`/`Locator` satisfy; `ui.ts#applyConsentThroughUi()` resiliently clicks the CMP accept/reject button by role + accessible name (`DEFAULT_CONSENT_UI_SELECTORS`: `/accept|aceptar/i` / `/reject|rechazar/i`, modeled on the legacy `setConsentThroughUi` regexes but not importing it), bounded `isVisible()` checks guarded with `.catch(() => false)` (no arbitrary `waitForTimeout`), and NEVER throws when the banner/button is absent (`interacted:false`); `isConsentBannerVisible()` reports presence via an optional `bannerTestId` or, falling back, accept/reject button visibility. `state-verifier.ts#verifyConsentState()` combines the #7 cookie signal (`adapter.verify()`) with `bannerDismissed` into a `ConsentStateReport` (`satisfied` true only when both hold, signals exposed for diagnosis, never the raw cookie value); `assertConsentState()` throws a normalized `state_verification_error`/`state_verification` `ConsentStateVerificationError` with a redacted message naming the failing signal(s) when not satisfied, never throwing when satisfied. Verified by `tests/unit/consent-state-verification.spec.ts` (accept/reject click routing, no-throw on absent/rejecting locators, banner visibility via test-id and button fallback, combined satisfied/fail-by-cookie/fail-by-banner cases, and a redaction test asserting a distinctive raw cookie value never appears in `JSON.stringify(report)` or `error.message`), `npm run typecheck` and `npm run lint` (both exit 0), `npm run test:unit` (all pass, no browser launched). Still pending: wiring `applyConsentThroughUi`/`verifyConsentState`/`assertConsentState` into real navigation/orchestration, config-schema-driven selectors, and true end-to-end verification against a live page — none of that exists yet. |
| Page stabilization | partial | `src/stability/` (new, additive — legacy `prepare-page.ts` untouched/unused, still has the `waitForTimeout(500)` this feature deliberately avoids): `page-like.ts` defines a minimal DI structural `StabilityPageLike` (`waitForLoadState`/`waitForFunction`/`emulateMedia`/`addStyleTag`/`evaluate` — deliberately no `waitForTimeout`) that a real Playwright `Page` satisfies. `policy.ts` defines `ReadinessPolicy`, `DEFAULT_READINESS_POLICY` (DOM `'load'`, fonts on, animations `'disabled'`, lazy-load 8 steps, freeze-time off, mask `['[data-visual-mask]']`, `timeoutMs: 10_000`) and `readinessPolicyFromConfig()` mapping `VisualPolicy.animations` (explicit overrides win). `readiness.ts#preparePage()` runs the SPEC-003 policy in order — DOM load state, an optional app-ready `waitForFunction`, fonts (`document.fonts.status === 'loaded'`), animation disabling (`emulateMedia({reducedMotion:'reduce'})` + narrow CSS that only turns off animations/transitions/caret, no element hiding), a single deterministic rAF-driven scroll-and-return-to-top lazy-load pass, and an opt-in time freeze that only runs when the page itself reports `window.__VISUAL_QA_FREEZE_TIME__` support — recording each step in `ReadinessResult.steps`/`timeFrozen`, with every bounded wait capped by `policy.timeoutMs` and any timeout/rejection re-thrown as a normalized `readiness_timeout`/`readiness` `ReadinessTimeoutError` naming the failing condition (e.g. "DOM load", "fonts", "app-ready signal", "lazy-load scroll", "freeze-time"); `resolveMaskSelectors()` exposes the declared mask selectors for reports. Verified by `tests/unit/page-readiness-policy.spec.ts` (DI fakes only, no real browser, covering ordered steps, animation on/off, fonts/app-ready gating, lazy-load count + return-to-top, freeze-time supported/unsupported, a named timeout per condition, mask-selector exposure and config mapping/overrides), `npm run typecheck` and `npm run lint` (both exit 0), `npm run test:unit` (all pass, no browser launched). Still pending (later features, #10): wiring `preparePage`/`resolveMaskSelectors` into a real snapshot/orchestration flow — nothing here captures a screenshot or applies masks yet — and mask selectors/the readiness policy remain code-level options, not `ProjectConfig` schema fields. |
| Visual baselines | partial | `src/visual/` (new, additive — legacy `tests/visual/` scaffold untouched): `target.ts` defines the `VisualTarget` discriminated union (`full-page`/`viewport`/`component`/`ad-placement`) and `targetId()`, a stable filesystem-safe slug (`full-page`, `viewport`, `component-<slug(name)>`, `ad-<slug(name)>`), throwing a normalized `configuration_error`/`planning` `VisualTargetError` for an empty name/selector or an empty resulting slug. `plan.ts#resolveTargetPlan()` is a pure function (no Date/random/env/fs) that combines a `VisualTarget`, the existing `VisualPolicy` (from `src/config/schema.ts`, not redefined), a `BaselinePartition` (`browser`/`platform`/`device`) and optional declared mask selectors + a narrow, justified `ThresholdOverride` into a `ScreenshotAssertionPlan` (`fullPage` true only for `full-page`, `clipSelector` set only for `component`/`ad-placement`, effective `maxDiffPixelRatio` = override ?? policy default, `animations` copied from policy, mask selectors deduped preserving first-seen order) plus a `VisualTargetMetadata` object for later embedding in `result.json` (threshold override, when applied, is preserved for human review); `baselineName()` partitions baseline names by browser/platform/device so cross-platform images never compare, and an override missing justification or outside `[0,1]` throws the same normalized `VisualTargetError`. This feature takes/compares no screenshots and writes no PNGs — it only produces the inputs a later snapshot-runner will hand to Playwright's `toHaveScreenshot` (#11+). Verified by `tests/unit/visual-targets.spec.ts` (covering all four target kinds' `fullPage`/`clipSelector` shapes, slug/targetId determinism and error paths, baseline-name partitioning across browser/platform/device, override rejection/application/preservation, and mask dedup/order), `npm run typecheck` and `npm run lint` (both exit 0), `npm run test:unit` (all pass, no browser launched). Still pending: wiring `resolveTargetPlan` into a real snapshot-runner/orchestration flow, a baseline-update/approval command (#11), and exposing `VisualTarget[]`/overrides as `ProjectConfig` schema fields (currently code-level inputs only). |
| Baseline update command | implemented | `src/baseline/` (new, additive): `paths.ts` defines the committed `baselines/<project>/<baselineName>.png` store plus `baselines/UPDATE_LOG.jsonl`, mirroring `src/artifacts/paths.ts`'s `assertSafeSegment`/`assertInside` traversal guards (rejects absolute/`..`/separator-injected segments). `plan.ts#planBaselineUpdate()` is a PURE function (no fs/Date/random/env — existence checks are injected via `baselineExists`/`sourceExists`) that requires a non-empty written `reason` (else throws a normalized `configuration_error`/`planning` `BaselineUpdateError`, mirroring `src/visual/target.ts`'s error-subclass pattern), rejects requests whose source `actual.png` is missing (never fabricating a baseline) instead of silently dropping them, and marks each remaining request `create`/`overwrite` deterministically in input order. `apply.ts#applyBaselineUpdate()` is the only impure layer: it applies `create`s unconditionally, skips `overwrite`s unless `opts.allowOverwrite` (`--yes`), copies via an injected `FileSystemLike` (`mkdirp`+`copyFile`) and appends exactly one secret-free JSON line (reason, ISO timestamp, tool version, optional commit sha, applied update ids) to the audit log via an injected `Clock`, only when >=1 update applied. `cli.ts` (`npm run baseline:update`) parses `--scenario`/`--target`/`--baseline-name`/`--source` (repeatable, paired positionally), `--project`, required `--reason`, `--yes`, `--dry-run`, `--json` and `--help`; real runs back the DI ports with `node:fs` and `new Date().toISOString()`, read `toolVersion` from `package.json`, and read `commitSha` from `process.env.GITHUB_SHA` when present; exit codes are `0` success/clean dry-run, `2` usage error, `3` nothing to do, `4` refused (overwrites present, `--yes` absent, nothing applied); it never launches a browser and never runs automatically. The committed `baselines/` store (with `.gitkeep` and a `README.md` explaining the PR+reason review requirement) is empty for now. Verified by `tests/unit/baseline-update-command.spec.ts` (hermetic, in-memory `FileSystemLike`/fixed `Clock`, no real fs I/O, no browser — covers reason-required, dry-run/plan purity, create/overwrite/missing-source handling, plan determinism, the single secret-free audit line, and all path-traversal guards), `npm run typecheck` and `npm run lint` (both exit 0), `npm run test:unit` (all pass). Deferred, per the SPEC's explicit non-goal: auto-discovery of update candidates from a completed run directory — the snapshot runner and `result.json` `baselineName`/target metadata don't exist yet, so this command is driven entirely by explicit `--scenario`/`--target`/`--source` input; a later feature will add discovery. |
| Diagnostics | partial | `src/diagnostics/` (new, additive — legacy `collectDiagnostics()` in `collector.ts` untouched/unused by new code, kept only so the existing `tests/visual/homepage.spec.ts` keeps compiling): `page-events.ts` defines minimal DI structural interfaces (`DiagnosticsPageLike`/`ConsoleMessageLike`/`RequestLike`/`ResponseLike`, overloaded `on(...)` per `DiagnosticsPageEvent`) a real Playwright `Page` satisfies, no Playwright import. `redact.ts` (SECURITY-CRITICAL, pure, no fs/Date/random/env) `redactHeaders()` drops `authorization`/`proxy-authorization`/`cookie`/`set-cookie`/`x-api-key`/`api-key`/`x-auth-token`/`authentication` case-insensitively (whole header dropped, not masked) preserving order of the rest; `redactUrl()` strips query/fragment, returning `origin+pathname` when parseable, else the substring before the first `?`/`#`, never throwing. `collector.ts#createDiagnosticsCollector(page, policy: DiagnosticsPolicy)` attaches listeners on construction and accumulates in insertion order: console only when `captureConsole`, page errors always, `requestfailed`/non-success (`status>=400`) `response` only when `captureNetwork` (responses `<400` ignored); a request/response whose ORIGINAL url host equals or is a subdomain of any `ignoredDomains` entry (case-insensitive) is dropped before storage; every stored url/header set passes through `redactUrl`/`redactHeaders`. `snapshot()` returns a deterministic, insertion-ordered deep copy isolated from later mutation on either side. `index.ts` barrels all three modules. Verified by `tests/unit/diagnostics-collector.spec.ts` (hand-written fake `DiagnosticsPageLike` driving synthetic events — no real browser — covering all four event types, `captureConsole`/`captureNetwork` gating, always-captured page errors, `ignoredDomains` host+subdomain matching, header/url redaction assertions and snapshot determinism/deep-copy isolation), `npm run typecheck` and `npm run lint` (both exit 0), `npm run test:unit` (440 passed across both Playwright projects). Still pending (later features, per SPEC-005 non-goals): writing `console.json`/`page-errors.json`/`requests.json` to disk, navigation/adapter-phase and timing-milestone capture, trace/video/screenshot-metadata capture, and wiring the collector into real scenario execution/orchestration — none of that exists yet. |
| Ad-state adapter | partial | `src/ads/` (new, additive, mirrors the SPEC-002 consent-adapter contract shape per `docs/features/ad-state-adapter/SPEC.md`, ticket #31): `adapter.ts` defines `AdState` (= `Scenario['adsEnabled']`), minimal DI structural interfaces `AdStateContextLike` (`addInitScript`)/`AdStateVerifyPageLike` (`evaluate`), the non-throwing `AdStateVerification` (`satisfied`/`present`/`expectedEnabled`) and `RedactedAdStateDescriptor`/`AdStateAdapter` contract (`apply`/`verify`/`describeRedacted`). `init-script-adapter.ts#InitScriptAdStateAdapter` implements the init-script strategy: `apply()` installs exactly one `context.addInitScript()` hook that sets `window[flagName] = enabled` (the boolean from `scenario.adsEnabled`) before any page script runs; `verify()` reads `window[flagName]` back via `page.evaluate()` and reports the *effective* state (`present` true only when the value is a boolean, `satisfied` true only when it strictly equals the expected boolean) — never assuming what was applied, never throwing. `describeRedacted()` returns `{strategy:'init-script', flagName, expectedEnabled}` (report-safe — only a hook name and a boolean, no secrets). `createAdStateAdapter(scenario, config)` resolves `flagName` from `config.adapters.ads.flagName` (default `'__ADS_ENABLED__'`) and the state from `scenario.adsEnabled`. `assertAdStateSatisfied()` throws a normalized `state_verification_error`/`state_verification` `AdStateVerificationError` naming the flag and expected state when not satisfied; `verify()` itself never throws. `src/config/schema.ts#AdsAdapterConfigSchema` gained an optional `flagName` and a `strategy` enum (currently only `'init-script'`) defaulting to `'init-script'`, still `.strict()` (unknown keys under `adapters.ads` still fail) and still fully defaulted (an empty `adapters.ads` still validates). Verified by `tests/unit/ad-state-adapter.spec.ts` (hand-written fake `AdStateContextLike`/`AdStateVerifyPageLike`, no real browser — covering init-script installation for both enabled states replayed against a fake `window`, verify's boolean-match/mismatch/absent/non-boolean cases, the factory's default-vs-configured `flagName` and scenario-driven state routing, `describeRedacted()`, the normalized-error assertion/no-throw-when-satisfied paths, and the two config-schema cases), `npm run typecheck` and `npm run lint` (both exit 0), `npm run test:unit` (530 passed across both Playwright projects). Explicitly NOT done here (per the SPEC's non-goals): wiring the adapter into `tests/visual/*` or `src/orchestrator/run-plan.ts`, and any placement container/geometry/request/render/layout-shift check (items 15-19) — this feature makes no claim about ad fill or revenue. |
| Placement contract | partial | `src/placements/` (new, additive, ticket #33, per `docs/features/placement-contract/SPEC.md`): `model.ts` defines the pure `PlacementDefinition` contract mirroring `docs/ads/PLACEMENT_MODEL.md` field list — `PlacementSizeSchema`, `PlacementVisibilitySchema` (`desktop`/`mobile`, both default `true`), `PlacementEventSignalsSchema` (optional `request`/`render` test-hook/event names), and `PlacementDefinitionSchema` (`.strict()`, `rejectSecretLikeKeys('PlacementDefinition')` — a local equivalent of the `src/domain/result.ts` guard) with `id`/`pages`/`containerSelector`/`allowedSizes` all non-empty, defaulted `visibility`/`timeoutMs` (10000)/`expectedEmpty` (false)/`protectedRegions` ([]), and optional `screenshotTarget`. `PlacementStageSchema`/`PlacementStateSchema` enumerate the state machine's stages/states, and `PlacementObservationSchema` (also secret-guarded) holds only `placementId`/`state`/`stage`/`terminal` — no raw response bodies, URLs, headers or vendor payloads. `state-machine.ts#classifyPlacement()` is a PURE function implementing the exact gate order from PLACEMENT_MODEL.md: `applicable===false`→`skipped`, `containerFound===false`→`container_missing`, `containerFound!==true`→non-terminal `container_ready`/container, `requestObserved===false`→`request_missing`, `requestObserved!==true`→non-terminal `container_ready`/request, each `renderOutcome`→its own terminal (`rendered`/`empty`/`provider_error`/`timeout`), else non-terminal `requested`/render — contradictory input (e.g. `containerFound:false` + `requestObserved:true`) resolves to the earlier gate's terminal. `TERMINAL_PLACEMENT_STATES` and every returned `observation.terminal` agree by construction. `isPlacementSatisfied()` implements the truth table (`skipped`/`rendered`→true, `empty`→`definition.expectedEmpty`, everything else→false). `PlacementDefinitionError extends Error` (mirrors `VisualTargetError`) carries a `normalized` `configuration_error`/`planning` `NormalizedError`; `parsePlacementDefinitionOrThrow()` catches the `ZodError` and builds its message from `error.issues` field PATHS only, never the supplied values. `index.ts` barrels both modules. No DOM/Playwright/fs/Date/random/env anywhere in this module. Verified by `tests/unit/placement-contract.spec.ts` (hermetic, no browser — definition defaults and every rejection case incl. a secret-like key; one assertion per state-machine path incl. both non-terminal boundaries and the contradictory-input case, with `terminal` checked against `TERMINAL_PLACEMENT_STATES` every time; the full `isPlacementSatisfied` truth table incl. `expectedEmpty` on/off; and `parsePlacementDefinitionOrThrow` asserting normalized category/phase and a field-path-only, value-free message), `npm run typecheck` and `npm run lint` (both exit 0), `npm run test:unit` (592 passed across both Playwright projects, up from 530). Explicitly NOT done here (per the SPEC's non-goals): any container/geometry/request/render/layout-shift observation (items 16-19), wiring `PlacementDefinition[]` into `ProjectConfig`/the scenario planner/orchestrator, and any DOM/browser work — this feature makes no claim about ad fill or revenue; it only classifies already-observed signals. |
| Ad placement checks | not implemented | Specification only. |
| Placement container checks | partial | `src/placements/container.ts` (new, additive, ticket #35, per `docs/features/placement-container-checks/SPEC.md`) is the first consumer of the placement contract (#33): minimal DI structural interfaces `PlacementBoundingBox`/`PlacementLocatorLike` (`count`/`first`/`isVisible`/`boundingBox`)/`PlacementContainerPageLike` (`locator`) that a real Playwright `Page`/`Locator` satisfy (no Playwright import). `readContainerState(page, definition)` is a single-shot, dependency-injected reader — `present = (await loc.count()) > 0` short-circuits to `{present:false, visible:false, box:null}` WITHOUT calling `isVisible`/`boundingBox` when absent; otherwise reads `first().isVisible()`/`first().boundingBox()` and rounds the box to integer `{width, height}` (`null` when not measurable) — no waiting/polling/timeouts. `evaluateContainer(definition, device, raw, options?)` is PURE (no fs/Date/random/env/DOM): resolves `expectedVisible = definition.visibility[device]` and an integer `tolerancePx` (default `1`); matches `raw.box` against `definition.allowedSizes` in order (`matchedSize` = first entry within tolerance on both dimensions, else `null`; `sizeAllowed = matchedSize !== null`); `containerFound = present && visible` and `applicableOnDevice = expectedVisible` are the two signals `classifyPlacement` (from #33, reused unmodified) consumes. `satisfied` follows the SPEC's per-device-expectation truth table: when expected visible, `containerFound && sizeAllowed`, with an ordered `reason` (`'container missing'` → `'container not visible'` → `` `size not allowed (observed WxH)` ``/`'size not allowed (not measurable)'`); when NOT expected visible, `satisfied = !visible` with `` `unexpectedly visible on <device>` `` (sizeAllowed still computed for diagnosis only). `PlacementContainerError extends Error` mirrors `src/visual/target.ts`'s pattern, carrying a `normalized` `placement_failure`/`assertion` `NormalizedError`; `assertContainer(observation)` throws it with a report-safe `` `Placement "<id>" container check failed: <reason>` `` message (never the selector value or page content) and never throws when satisfied. Optional pure helper `toContainerSignals(observation)` projects `{applicable, containerFound}` for `classifyPlacement`. `index.ts` barrels the new module. Verified by `tests/unit/placement-container-checks.spec.ts` (hermetic, no browser — fake locator/page with call counters proving `isVisible`/`boundingBox` are skipped on absence, present/rounding/null-box paths, `evaluateContainer` truth tables for both visibility expectations incl. the exact/`tol`/`tol+1` tolerance boundary and first-match selection across multiple `allowedSizes`, every ordered `reason`, a `classifyPlacement` integration check for `container_ready`/`container_missing`/`skipped`, and `assertContainer` throw/no-throw plus a value-leak assertion that a distinctive `'#SECRET-SELECTOR-xyz'` selector never appears in the observation or error message), `npm run typecheck` and `npm run lint` (both exit 0), `npm run test:unit` (632 passed across both Playwright projects, up from 592). Explicitly NOT done here (per the SPEC's non-goals): request/render event capture and layout-shift (items 17-19), overlap-with-protected-content checks, wiring into the scenario planner/orchestrator/visual suite or exposing `PlacementDefinition[]` via `ProjectConfig`, and any waiting/polling/timeout behaviour — this feature makes no claim about ad fill or revenue. |
| Placement request events | partial | `src/placements/events.ts` (new, additive, ticket #37, per `docs/features/placement-request-events/SPEC.md`) is a GENERIC, dependency-injected `PlacementEventsCollector` — deliberately generic so item 18 (render events) reuses it unchanged. `createPlacementEventsCollector(source)` registers exactly ONE `on('placement', ...)` handler on construction (mirroring `src/diagnostics/collector.ts`'s subscribe-at-construction pattern and `src/diagnostics/page-events.ts`'s DI `on(...)` event-source style); each incoming event is normalized to EXACTLY `{ placementId, signal }` — extra fields (e.g. a `url`/`payload`) are dropped defensively, never spread, so no secret/vendor payload can enter the accumulated events — and pushed in insertion order. `snapshot()` returns a deep copy (new array + new objects) isolated from later mutation on either side; `countMatching(placementId, signal)` counts exact (case-sensitive) matches, duplicates included. `src/placements/request.ts` adds the PURE `evaluateRequest(definition, collector)`: maps `definition.events?.request ?? null` to a `RequestObservation` — when no request signal is configured, `requestObserved` stays `undefined` (not `false`) precisely so `classifyPlacement` (from #33, reused unmodified) remains at the non-terminal `container_ready` boundary instead of a false `request_missing` verdict; when configured, `count = collector.countMatching(id, signal)` and `requestObserved = count > 0`. `toRequestSignals(observation)` projects `{ requestObserved }` for `classifyPlacement`. `index.ts` barrels both new modules. Verified by `tests/unit/placement-request-events.spec.ts` (hermetic, no browser — a fake `PlacementEventSourceLike` with a registration counter proving exactly one registration, insertion-order accumulation, deep-copy isolation in both directions, extra-field stripping asserted via `Object.keys`, the `countMatching` matrix, all three `evaluateRequest` cases plus `toRequestSignals`, a `classifyPlacement` integration check for `requested`/`request_missing`/`container_ready`, and a privacy assertion that no snapshot/observation JSON contains a URL or the string `token`), `npm run typecheck` and `npm run lint` (both exit 0), `npm run test:unit` (656 passed across both Playwright projects, up from 632). Explicitly NOT done here: render/completion evaluation (item 18, though it reuses this same collector), layout-shift (item 19), overlap/`protectedRegions`, container checks (item 16, already covered by #35), any fill/revenue claim, raw network/vendor payload capture, and wiring into the scenario planner/orchestrator/config (the real event bridge backing `PlacementEventSourceLike` is a later wiring ticket). |
| Country override | not implemented | Requires an internal QA mechanism or proxy decision. |
| AI provider abstraction | partial | Interface and Anthropic example exist; robust payload, redaction, retries and reporting are incomplete. |
| JSON/HTML summary | partial | `src/reporting/` (new, additive): `aggregate.ts#computeCounts()` tallies `passed`/`failed`/`skipped` from `ScenarioResult[]` (`total` always equals the sum and the input length). `buildRunResult()` assembles counts + the existing `computeDeterministicFailure()`/`parseRunResult()` from `src/domain` into a schema-valid `RunResult`, preserving input order and never mutating its input. `groupFailuresByPage()` buckets every result that counts as a failure (status `'failed'` OR any non-`'warning'`-severity error; `'skipped'` and clean `'passed'` excluded) into per-page groups ordered by first appearance, choosing the "probable category" as the first non-warning error's category, else the first error's category, else the documented `'unknown_error'` fallback (with the fixed message `'Scenario failed without a normalized error'` when a failed result carries no errors). `cli-summary.ts#formatCliSummary()` renders a concise, secret-free, multi-line summary (counts, PASS/FAIL, and a "Failures by page" block listing categories only — never raw messages or query strings) and `exitCodeForRun()` returns `0` unless `RunResult.deterministicFailure` is `true` (AI-provider-only warnings never flip it). Pure, deterministic, no fs/Date/random/env. Verified by `tests/unit/run-summary.spec.ts` (hermetic `ScenarioResult`/`RunManifest` fixtures built with `normalizeError`, no browser/fs), `npm run typecheck` and `npm run lint` (both exit 0), `npm run test:unit` (all pass across both Playwright projects). Still pending: writing `result.json`/`report/index.html` to disk (the artifacts writer from #5 does files; HTML rendering is a later feature) and wiring this aggregation into a real orchestrator run. |
| URL discovery | not implemented | Specification only. |
| Local dashboard | not implemented | Future scope. |
| End-to-end visual run wiring | partial | `feature/end-to-end-run` (#29) wires the Browser MVP modules into a runnable flow and retires the legacy scaffold. `src/orchestrator/run-plan.ts#buildVisualRunPlan()` is a PURE, deterministic expansion (no fs/Date/random/env, no browser, input order preserved) of `planScenarios(config)` output into a `VisualRunPlan`: each `ScenarioWorkItem` carries the safely joined `url` (`baseUrl`+`page.path`, exactly one `/`, query/hash preserved), the `readinessPolicyFromConfig(config)` policy, and at least a default `{ kind: 'full-page' }` target with a deterministic `baselineName(target, { browser:'chromium', platform:'ci', device })` (stable logical partition; the real per-project partition is applied by Playwright's `snapshotPathTemplate`). `tests/visual/scenarios.spec.ts` (replaces the deleted `homepage.spec.ts`) consumes the plan and drives ONLY the new modules — cookie consent adapter (`apply` before navigation), diagnostics collector, `page.goto(domcontentloaded)`, `preparePage(readiness)`, `verifyConsentState` (asserting only the cookie signal, so an unknown site with no CMP banner does not spuriously fail — the screenshot stays authoritative), and `toHaveScreenshot` per target with config threshold/animations and readiness mask selectors. `playwright.config.ts` gains `snapshotPathTemplate: 'baselines/{projectName}/{arg}{ext}'` so screenshots read/write the committed `baselines/` store. `src/orchestrator/{cli,orchestrator,types}.ts` now plan via `loadConfig()`+`planScenarios`+`buildVisualRunPlan` and spawn `playwright test tests/visual` (keeping `VISUAL_SCENARIOS` selection and `--update` support). Legacy `src/scenarios/scenarios.ts`, `src/consent/consent-manager.ts`, `src/stability/prepare-page.ts` and `tests/visual/homepage.spec.ts` are DELETED and no source imports them (grep-clean; only a comment in `src/consent/ui.ts` still names the old file). Verified: `npm run typecheck`, `npm run lint` both exit 0; `npm run test:unit` (496 passed across both Playwright projects) includes hermetic `tests/unit/end-to-end-run-plan.spec.ts` covering URL join (trailing/no/embedded slash, query/hash, root), default full-page target, baseline-name determinism, readiness wiring, `targetsFor` override/empty fallback, input-order preservation and input non-mutation. The visual spec is TYPECHECKED but NOT executed by required CI — it needs a real browser + committed baselines and runs only in the gated `visual` `workflow_dispatch` job or on an unrestricted machine (the local macOS sandbox cannot spawn page renderers). Still pending before `implemented`/`verified`: a real QA `BASE_URL`/`QA_BASE_URL`, a first REVIEWED baseline commit, and an actual green visual run in CI — see `docs/DEMO.md`. |
| CI | partial | `.github/workflows/ci.yml` runs on every PR/push as ordered jobs (fail fast, cheapest first): `static` (`npm ci` → `typecheck` → `lint`), `unit` (`test:unit` = hermetic `tests/unit`, installs only the chromium binary the runner needs to load config; launches no browser), and `browser-integration` (`npx playwright install --with-deps chromium` → `test:integration` = the real-Chromium smoke test from #6 that could not run in the local sandbox). A `visual` job exists but is gated behind `workflow_dispatch` and a `QA_BASE_URL` secret (it fails fast if the secret is missing) so it never red-Xes normal PRs until a QA target + committed baselines exist; it uploads `reports/html` as an artifact. `test:unit`/`test:integration` npm scripts back both CI and local runs. Verified locally: `npm run typecheck`, `npm run lint`, `npm run test:unit` (206 passed). The workflow's runtime (unit/integration/visual on GitHub runners) will be confirmed on the first PR run. Still external/pending: browser binary caching, branch-protection required-checks config, and wiring the visual job into required checks once baselines land. |

## Rule for updates

Every feature PR must update this table. A capability becomes `verified` only after its acceptance tests run successfully in the intended environment.
