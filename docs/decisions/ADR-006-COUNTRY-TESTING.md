# ADR-006 Country testing strategy

**Status:** Proposed pending Hola environment details.

Prefer an internal, test-only country override with an observable debug signal. Use proxies only when the real network location is required. Locale/geolocation alone must not be labelled country simulation.
