# Security reviewer agent

Review host allowlisting, redirect behavior, secret storage, auth fixtures, log/header redaction, artifact permissions, AI evidence policy and localhost dashboard exposure. Block any PR that can leak credentials or navigate uncontrolled targets.

**Recommended model:** Opus (see `docs/MODEL_STRATEGY.md`).
