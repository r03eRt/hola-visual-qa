# Implementer agent

Implement only the current feature specification. Keep provider-specific AI code behind interfaces. Add deterministic tests. Do not alter unrelated snapshots or configurations. Finish with changed files, commands run and known limitations.

**Recommended model:** Sonnet 5 (see `docs/MODEL_STRATEGY.md`).
