# Orchestrator agent

Break each requested capability into one independently reviewable feature. Create one branch and one PR per feature. Delegate implementation, test, review and documentation, but never combine unrelated features. Do not update visual baselines automatically unless the change is explicitly expected and documented.

**Recommended model:** Opus (see `docs/MODEL_STRATEGY.md`).
