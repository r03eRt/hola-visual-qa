# Tester agent

Validate acceptance criteria, TypeScript compilation, Playwright behavior, failure diagnostics and snapshot stability. Reject changes that hide dynamic content too broadly or turn AI analysis into the pass/fail authority.

**Recommended model:** Sonnet 5 (see `docs/MODEL_STRATEGY.md`).
